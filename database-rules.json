{
  "rules": {
    "users": {
      ".read": "auth != null",
      "$uid": {
        ".read": "auth != null",
        ".write": "$uid === auth.uid",
        ".validate": "newData.hasChildren(['email']) && (newData.hasChild('fullname') || newData.hasChild('photoURL'))",
        "fullname": {
          ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 100"
        },
        "email": {
          ".validate": "newData.isString() && newData.val().contains('@')"
        },
        "createdAt": {
          ".validate": "newData.isString()"
        },
        "photoURL": {
          ".validate": "newData.isString() || newData.val() === null"
        },
        "description": {
          ".validate": "newData.isString() && newData.val().length <= 150"
        }
      }
    },
    "reels": {
      ".read": "auth != null",
      ".write": "auth != null",
      "$reelId": {
        "userId": { ".validate": "newData.isString()" },
        "userName": { ".validate": "newData.isString()" },
        "userPhoto": { ".validate": "newData.isString()" },
        "caption": { ".validate": "newData.isString() && newData.val().length <= 300" },
        "mediaUrl": { ".validate": "newData.isString()" },
        "mediaType": { ".validate": "newData.isString() && (newData.val() === 'image' || newData.val() === 'video')" },
        "timestamp": { ".validate": "newData.isNumber()" },
        "likes": {
          "$userId": { ".validate": "newData.isBoolean()" }
        },
        "comments": {
          "$commentId": {
            ".read": "auth != null",
            ".write": "auth != null",
            ".validate": "newData.hasChildren(['userName', 'userPhoto', 'text', 'timestamp', 'userId']) && newData.child('userId').val() === auth.uid",
            "userName": { ".validate": "newData.isString()" },
            "userPhoto": { ".validate": "newData.isString()" },
            "text": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 500" },
            "timestamp": { ".validate": "newData.isNumber()" },
            "userId": { ".validate": "newData.isString() && newData.val() === auth.uid" }
          }
        }
      }
    },
    "chats": {
      ".read": "auth != null",
      ".write": "auth != null",
      "$chatId": {
        ".read": "auth != null",
        ".write": "auth != null",
        "messages": {
          "$messageId": {
            ".write": "auth != null",
            ".validate": "newData.hasChildren(['senderId', 'senderName', 'timestamp'])",
            "senderId":   { ".validate": "newData.isString()" },
            "senderName": { ".validate": "newData.isString()" },
            "text":       { ".validate": "newData.isString()" },
            "timestamp":  { ".validate": "newData.isNumber()" },
            "isRead":     { ".validate": "newData.isBoolean()" },
            "isDeleted":  { ".validate": "newData.isBoolean()" },
            "isUnsent":   { ".validate": "newData.isBoolean()" },
            "mediaUrl":   { ".validate": "newData.isString()" },
            "mediaType":  { ".validate": "newData.isString() && (newData.val() === 'image' || newData.val() === 'video')" },
            "deletedFor": {
              "$userId": { ".validate": "newData.isBoolean()" }
            }
          }
        }
      }
    },
    "notes": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "presence": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
